# دفعة تعديلات 2026-08-19 — spec (locked)

## PROGRESS (handoff 2026-08-19, session 1 — nothing committed, all in working tree)
- ✅ PR 1 copy renames («تسجيل» gone, «المستحقات»→«الديون», «رقم الحساب» hidden) — green.
- ✅ PR 2 قائمة الأسعار removed + all prefills — typecheck/lint/test/verify/e2e green.
- ✅ PR 3 medical flags (migration drizzle/0002) + patient filters — e2e 24/24 incl. new patients-medical.spec.
- ✅ PR 4 ortho no-total — verify 37/37; ortho excluded from debts/pickers; live math on both ortho forms.
- ✅ PR 5 treatment free text + daily-entry live math — **finished session 2**. The in-flight
  agent had landed the server side (findOrCreateTreatmentType, normalTreatmentSuggestions,
  `treatmentName` action, `#nc-treatment` input+datalist) but left the payment tab broken:
  live-math state was added without its `PAYMENT_KINDS`/`PaymentKind` declarations or any
  wiring into the inputs, `openCasesBrief` mapping dropped `remaining`, and the e2e
  selectOption→fill contract change was never made. All four fixed; gates green.
- ⏳ PR 6 (calendar), PR 7 (doctors+labs), PR 8 (icons+sweep) — not started.
- Gate status 2026-08-19 session 2: typecheck ✅ · lint ✅ · test 27/27 ✅ · verify 37/37 ✅ ·
  e2e 24/24 ✅ · build ✅. NOTE: `npm rebuild better-sqlite3` was needed first — the laptop's
  Node moved to v26 and the native module was still built for the old ABI. Re-run that after
  any Node upgrade or `npm test` dies with ERR_DLOPEN_FAILED.
- The first e2e run of a session can flake on /settings (RTL assertion timeout) when memory is
  tight; a re-run on a quiet machine is 38s vs 1.1m. Not a code fault.
- Done outside chunks: `components/forms/money-summary.tsx` (shared live-math line); `shiftPeriod`+`shiftISOByDays/Months` consolidated into lib/dates.ts (expenses/cash/settlement pages migrated; **xrays/page.tsx still has a local copy** — fold into PR 8).
- PR 8 additions collected during build: demo data (lib/db/demo.ts) should include patients with medical flags; `patientsList({hasBalance})` + /patients/[id] per-case المتبقي still count LEGACY ortho totals — exclude bucket ortho there too; MoneySummary still missing on debts pay-dialog, implants new-card/card-actions, cash form; xrays shiftPeriod; exit greps per PR 8.
- Gates per chunk: `npm run typecheck && npm run lint && npm test && npm run verify && npm run e2e`.
- Build strategy: one opus subagent per chunk with the chunk section pasted + golden rules + «no تسجيل/المستحقات in copy» + data-tour/e2e contracts reminder; verify gates inside the agent.

Batch of client-requested changes. Decisions below were confirmed with the client rep on 2026-08-19.
Execute in order; every chunk ends green: `npm run typecheck && npm test` (+ `npm run verify` / `npm run e2e` where noted).

## Locked decisions
- **Labs:** EVERY doctor has his own lab (name editable, e.g. علي → «دوبرا»). Lab money = **standalone entries** on the doctor page (date, amount, note, optional patient). Each lab has **two branches: ثابت + متحرك** — every entry is tagged, both totals shown.
- **Lab money is TRACK-ONLY:** never touches clinic cash, never enters settlement payout. Shown on doctor pages + one info line in settlement. (Integrated option documented in OWNER-NOTES as a future switch.)
- **Access:** one shared PIN stays. No roles. Doctor pages open to anyone logged in; secretary edits everything.
- **Ortho: agreed total REMOVED COMPLETELY.** Ortho money = المقدمة (initial amount) + each session its own amount. No الإجمالي, no المتبقي anywhere in ortho UI. Ortho never appears in debts.
- **«تسجيل» removed everywhere** in visible copy (reworded: إضافة/حفظ/حجز…).
- **«المستحقات» → «الديون»** (patient balances). The word «مستحقات» is now reserved for DOCTOR dues.
- **قائمة الأسعار removed** (nav + page + prefills). Prices typed manually per case. `price_list` table stays dormant.
- **Treatment in daily entry = free text** with saved suggestions (find-or-create treatment type, bucket `normal`; non-normal names refused → protects D9).
- **Live math in every money form** (see cross-cutting item below).

## Chunks

### PR 1 — Copy renames + small removals (no schema)
1a. Reword every «تسجيل» in UI copy (buttons/titles/toasts/tour/help/server error strings). Table in plan-agent output; core: «تسجيل دفعة»→«إضافة دفعة», «تسجيل أشعة»→«أشعة جديدة», «تسجيل الحضور/عدم الحضور»→«حضر»/«لم يحضر», «تسجيل الصرف»→«صرف الحصة», «تسجيل حركة نقدية»→«حركة نقدية جديدة».
1b. «المستحقات»→«الديون»: lib/strings.ts:131 nav, debts/page.tsx, dashboard StatCard+link, xray-form note, tour.ts, help. Route stays `/debts`. Reverse the «never دَين» rule in docs/CONVENTIONS.md + note in OWNER-NOTES §3.
1c. Remove «رقم الحساب» from implants UI only (list column, search placeholder + implantIndex filter arm queries.ts:317, [id] Field, tour). Keep DB column + counter (device precedent, OWNER-NOTES §4-style note).

### PR 2 — Remove قائمة الأسعار + all price prefills
Delete app/(app)/prices/ + lib/actions/prices.ts + mutations.setPrice. Remove nav item (strings.ts:134) + Tags icon + tour + help section. Daily entry-dialog: no defaultPrice prefill (price input starts empty). X-ray form: السعر/المدفوع الآن start empty, no prefill on type change. listTreatmentTypes: drop price_list join once unconsumed. seed.ts stops seeding price rows; keep AUDIT_ENTITY_LABELS.price_list. e2e smoke: remove /prices entry.

### PR 3 — Patients: chronic diseases + filters (migration M1)
Schema: patients.medicalFlags TEXT (JSON array of enum keys) + medicalNotes TEXT.
Flags (lib/strings.ts MEDICAL_FLAGS): heart قلب، diabetes سكري، hypertension ضغط الدم، allergy حساسية، bleeding سيولة الدم، asthma ربو، pregnancy حمل، kidney أمراض الكلى، hepatitis التهاب الكبد.
Mutations create/update/findOrCreatePatient gain the fields; action zod z.array(z.enum). UI: checkbox grid + textarea in patient-form; amber MedicalBadge chip (components/ui/medical-badge.tsx) on patients list, patient [id] alert, case pickers (xray search labels, openCasesBrief), ortho/implant [id] headers.
Filters: queries.patientsList({q, doctorId, hasBalance, visitedWithinDays}) via exists-subqueries on cases/payments; GET selects on patients page.

### PR 4 — Ortho: no total, per-session amounts
- ortho-forms.tsx: fields = patient, phone, doctor, **المقدمة**, date, next appointment. No total, no discount.
- actions/ortho.ts createOrthoCase: always totalPrice=0, listPrice=0, discount=0; مقدمة inserted as down_payment.
- mutations recordCasePayment/createCaseWithPayment: skip exceeds_remaining / amount>total guards when ortho bucket && totalPrice===0. updateCaseMeta total guard stays for legacy rows.
- ortho/page.tsx columns: المريض/الطبيب/التاريخ/**المقدمة**/**المدفوع**/الموعد القادم/ملاحظة (drop الإجمالي/المتبقي).
- ortho/[id]: money card = المقدمة + المدفوع only; payment form never disabled by remaining (only by status); meta-form unchanged (no total field).
- debtsList + dashboardStats.outstanding + openCasesBrief: **exclude bucket 'ortho' explicitly** (ortho never a debt, incl. legacy rows with totals).
- tests/ortho-open-total.test.ts: session+down payments accepted with total 0; debts exclude ortho; refund still capped by paid.

### PR 5 — Daily-entry treatment as free text
mutations.findOrCreateTreatmentType(nameAr): normalize, case-insensitive match vs ALL active types; non-normal bucket match → typed failure (existing redirect errors, D9 guard); else insert bucket normal (key custom_<ts>), audited (+AUDIT_ENTITY_LABELS.treatment_types). queries.normalTreatmentSuggestions() ordered by recency, limit 30. visits action takes treatmentName string. entry-dialog: `#nc-treatment` stays the id but becomes `<Input list="nc-treatment-list">` + datalist. e2e settlement.spec: selectOption → fill("جسر").

### PR 6 — Appointments: month calendar + filters + quick rebook
Saturday-first grid (ar-IQ firstDay=6), hand-built month-grid.tsx server component; ?view=month|day (&doctorId&status filters server-side in appointmentsForDate); appointmentCountsForMonth(period); upcomingAppointments list; rebook chips (+أسبوع/+شهر/+شهرين/+٣ أشهر) in booking dialog + per-row إعادة حجز server action (reads appt, createAppointment at shifted date). Lift shiftPeriod into lib/dates.ts. Day view stays default (data-tour="appt-list" untouched); month view data-tour="appt-month".

### PR 7 — Doctors section + Labs (migration M2)
Schema: doctors.labName TEXT; new lab_entries(id, doctorId, branch 'fixed'|'mobile', entryDate, amount signed, note, patientId nullable, createdAt).
server-utils.recordEdit gains markStale?:boolean (default true); lab mutations use markStale:false (late lab entry must NOT flip a closed settlement stale — lab money is outside payout).
Mutations: createDoctor, updateDoctor+labName, addLabEntry, deleteLabEntry. Queries: doctorById, labEntriesForDoctor(doctorId, period), labDuesForPeriod(period).
Nav: {href:"/doctors", label:"الأطباء", icon:"Stethoscope"} after المرضى (count stays 15 since prices left). LAB_BRANCH_LABELS ثابت/متحرك.
/doctors: month selector; computeSettlement(period) ONCE (reuse — never reimplement the formula) + labDuesForPeriod; card per doctor: مستحقات العيادة (payout) + مستحقات المختبر (ثابت+متحرك) + الصافي (info). Add-doctor dialog. data-tour="doctors-list".
/doctors/[id]: owner → clinic-wide overview first (all doctors + clinicNet) then own summary; everyone → info form (name/نسبة/اسم المختبر), dues breakdown per bucket from DoctorSettlement, lab entries add-form + month table + delete. data-tour="doctor-dues", "lab-entries".
Settlement page: info line «مستحقات المختبرات (تتبّع — خارج الحصص)».
tests/lab-dues.test.ts incl. closed-period entry does NOT mark stale. e2e smoke + tour + help + OWNER-NOTES (incl. integrated-option switch doc).

### PR 8 — Icons pass + live-math sweep + docs
Icons: page headers get their nav icon beside h1; dashboard StatCards small icons; empty states large muted icon; audit buttons.
Exit greps: «تسجيل», «المستحقات», «قائمة الأسعار», «رقم الحساب» clean in app/ lib/.
Docs: CLAUDE.md seed note, OWNER-NOTES consolidated, BRIEF addendum.
Final: typecheck && lint && test && verify && e2e && build.

## Cross-cutting — LIVE MATH in every money form (client request)
Reusable client component `components/forms/money-summary.tsx` (display-only; server stays authoritative; parseAmount/formatIQD), pattern = xray-form.tsx:280-291 live line. Apply as each form is touched, verify all in PR 8:
- daily entry «علاج جديد»: الصافي = السعر − الخصم; المتبقي = الصافي − المدفوع الآن.
- daily entry «دفعة على علاج سابق»: رصيد الحالة قبل → بعد الدفعة.
- debts pay-dialog: المتبقي بعد هذه الدفعة.
- ortho new-case: المقدمة = الدفعة الأولى (shown plainly).
- ortho session payment: مجموع المدفوع بعد هذه الدفعة.
- implants new-card + card-actions: الصافي (سعر − خصم) والمتبقي بعد المقدمة/التعديل.
- xray form: keep existing live line (loses prefill only).
- cash form: الرصيد الناتج (optional).
- settlement payout confirm: already shows math — verify.

## Risks (watch during build)
1. updateCaseMeta total≥paid guard error must be clear if a legacy ortho total is edited below paid.
2. D4: recordEdit markStale decoupling — only lab entries use false; everything else unchanged.
3. e2e data-tour contracts: prices-list out, doctors-list in, #nc-treatment select→input — each in the same chunk as its UI change; keep lib/tour.ts in sync.
4. Free-text treatments must never recreate implant/ortho/xray names as normal bucket (D9 bypass).
